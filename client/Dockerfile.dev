# React 前端开发环境 Dockerfile
FROM node:20-alpine

# 设置工作目录
WORKDIR /app

# 复制 client package.json
COPY client/package*.json ./

# 安装依赖
RUN npm install

# 复制 client 源代码
COPY client ./

# 暴露 Vite 端口
EXPOSE 5173

# 开发模式启动
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
